# Claude Code instructions

Repo conventions live in [`AGENTS.md`](./AGENTS.md) — single source
of truth across all AI coding agents. Read it first.

For test work, the Claude Code skill at
`.claude/skills/writing-tests/SKILL.md` auto-loads when you start
editing files under `__tests__/` or matching `*.test.{ts,tsx,js}`.
